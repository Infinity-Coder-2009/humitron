name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-mock ruff
      
      - name: Run linting (ruff)
        run: ruff check .
      
      - name: Run formatting check (black)
        run: black --check .
      
      - name: Run type checking (mypy)
        run: |
          pip install mypy
          mypy humitron_core.py --ignore-missing-imports || true
      
      - name: Run tests
        run: pytest test_humitron.py -v --tb=short
      
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.11'
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella

  # Future: Frontend CI (when frontend/ exists)
  # frontend-test:
  #   name: Frontend Tests
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #         cache-dependency-path: frontend/package-lock.json
  #     - run: cd frontend && npm ci
  #     - run: cd frontend && npm run test
  #     - run: cd frontend && npm run lint
  #     - run: cd frontend && npm run build

  # Future: Build Tauri/Electron app
  # build:
  #   name: Build App
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     matrix:
  #       os: [ubuntu-latest, macos-latest, windows-latest]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #     - uses: dtolnay/rust-toolchain@stable
  #       # For Tauri
  #     - run: cd frontend && npm ci && npm run tauri build
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: humitron-${{ matrix.os }}
  #         path: frontend/target/release/bundle/